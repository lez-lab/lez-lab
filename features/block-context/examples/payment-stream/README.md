# Example: Payment Stream Deadline

A payment stream program that uses `block_context_account` to enforce an `open_stream_by` deadline.
The stream creation fails if `block_index` exceeds the deadline at execution time.

> Full example coming once the block-context branch is merged upstream.
